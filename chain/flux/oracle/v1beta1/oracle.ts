// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: flux/oracle/v1beta1/oracle.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface SimpleEntry {
  symbol: string;
  decimal: string;
  value: string;
  timestamp: string;
}

function createBaseSimpleEntry(): SimpleEntry {
  return { symbol: "", decimal: "0", value: "", timestamp: "0" };
}

export const SimpleEntry: MessageFns<SimpleEntry, "flux.oracle.v1beta1.SimpleEntry"> = {
  $type: "flux.oracle.v1beta1.SimpleEntry" as const,

  encode(message: SimpleEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    if (message.decimal !== "0") {
      writer.uint32(16).int64(message.decimal);
    }
    if (message.value !== "") {
      writer.uint32(26).string(message.value);
    }
    if (message.timestamp !== "0") {
      writer.uint32(32).uint64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SimpleEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSimpleEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.decimal = reader.int64().toString();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.value = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.timestamp = reader.uint64().toString();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SimpleEntry {
    return {
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      decimal: isSet(object.decimal) ? globalThis.String(object.decimal) : "0",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "0",
    };
  },

  toJSON(message: SimpleEntry): unknown {
    const obj: any = {};
    if (message.symbol !== undefined) {
      obj.symbol = message.symbol;
    }
    if (message.decimal !== undefined) {
      obj.decimal = message.decimal;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp;
    }
    return obj;
  },

  create(base?: DeepPartial<SimpleEntry>): SimpleEntry {
    return SimpleEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SimpleEntry>): SimpleEntry {
    const message = createBaseSimpleEntry();
    message.symbol = object.symbol ?? "";
    message.decimal = object.decimal ?? "0";
    message.value = object.value ?? "";
    message.timestamp = object.timestamp ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
