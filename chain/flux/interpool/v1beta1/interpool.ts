// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.178.0
//   protoc               unknown
// source: flux/interpool/v1beta1/interpool.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";

export interface CommissionConfig {
  management_fee_rate: string;
  management_fee_interval: string;
  trading_fee_rate: string;
}

export interface CommissionFees {
  /** commission fee coins, separated for easy tracking, tests */
  management_fees: string[];
  trading_fees: string[];
}

/** Interpool */
export interface InterPool {
  pool_id: Uint8Array;
  operator_addr: string;
  /** on-going assets */
  inventory_snapshot: string[];
  /** initial assets before any trades from the pool */
  base_capital: string[];
  operator_commission_config: CommissionConfig | undefined;
  operator_commission_fees:
    | CommissionFees
    | undefined;
  /** flow control data for cron service */
  input_blob: Uint8Array;
  /** pool extra state for lp, reward tokens created by cron service */
  output_blob: Uint8Array;
  /** cron job controlling the pool */
  cron_id: Uint8Array;
  /** pool account */
  pool_account: string;
  next_commission_time: string;
  solver_id: Uint8Array;
}

export interface PoolShare {
  pool_id: Uint8Array;
  liquidity_provider_addr: string;
  /** on-going assets of users */
  asset_snapshot: string[];
  /** initial assets of users */
  base_capital: string[];
  /** ownership percentage */
  shares: string;
}

function createBaseCommissionConfig(): CommissionConfig {
  return { management_fee_rate: "0", management_fee_interval: "0", trading_fee_rate: "0" };
}

export const CommissionConfig = {
  $type: "flux.interpool.v1beta1.CommissionConfig" as const,

  encode(message: CommissionConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.management_fee_rate !== "0") {
      writer.uint32(8).int64(message.management_fee_rate);
    }
    if (message.management_fee_interval !== "0") {
      writer.uint32(16).int64(message.management_fee_interval);
    }
    if (message.trading_fee_rate !== "0") {
      writer.uint32(24).int64(message.trading_fee_rate);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CommissionConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommissionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.management_fee_rate = longToString(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.management_fee_interval = longToString(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.trading_fee_rate = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommissionConfig {
    return {
      management_fee_rate: isSet(object.management_fee_rate) ? globalThis.String(object.management_fee_rate) : "0",
      management_fee_interval: isSet(object.management_fee_interval)
        ? globalThis.String(object.management_fee_interval)
        : "0",
      trading_fee_rate: isSet(object.trading_fee_rate) ? globalThis.String(object.trading_fee_rate) : "0",
    };
  },

  toJSON(message: CommissionConfig): unknown {
    const obj: any = {};
    if (message.management_fee_rate !== undefined) {
      obj.management_fee_rate = message.management_fee_rate;
    }
    if (message.management_fee_interval !== undefined) {
      obj.management_fee_interval = message.management_fee_interval;
    }
    if (message.trading_fee_rate !== undefined) {
      obj.trading_fee_rate = message.trading_fee_rate;
    }
    return obj;
  },

  create(base?: DeepPartial<CommissionConfig>): CommissionConfig {
    return CommissionConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CommissionConfig>): CommissionConfig {
    const message = createBaseCommissionConfig();
    message.management_fee_rate = object.management_fee_rate ?? "0";
    message.management_fee_interval = object.management_fee_interval ?? "0";
    message.trading_fee_rate = object.trading_fee_rate ?? "0";
    return message;
  },
};

function createBaseCommissionFees(): CommissionFees {
  return { management_fees: [], trading_fees: [] };
}

export const CommissionFees = {
  $type: "flux.interpool.v1beta1.CommissionFees" as const,

  encode(message: CommissionFees, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.management_fees) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.trading_fees) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CommissionFees {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommissionFees();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.management_fees.push(reader.string());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.trading_fees.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommissionFees {
    return {
      management_fees: globalThis.Array.isArray(object?.management_fees)
        ? object.management_fees.map((e: any) => globalThis.String(e))
        : [],
      trading_fees: globalThis.Array.isArray(object?.trading_fees)
        ? object.trading_fees.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: CommissionFees): unknown {
    const obj: any = {};
    if (message.management_fees?.length) {
      obj.management_fees = message.management_fees;
    }
    if (message.trading_fees?.length) {
      obj.trading_fees = message.trading_fees;
    }
    return obj;
  },

  create(base?: DeepPartial<CommissionFees>): CommissionFees {
    return CommissionFees.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CommissionFees>): CommissionFees {
    const message = createBaseCommissionFees();
    message.management_fees = object.management_fees?.map((e) => e) || [];
    message.trading_fees = object.trading_fees?.map((e) => e) || [];
    return message;
  },
};

function createBaseInterPool(): InterPool {
  return {
    pool_id: new Uint8Array(0),
    operator_addr: "",
    inventory_snapshot: [],
    base_capital: [],
    operator_commission_config: undefined,
    operator_commission_fees: undefined,
    input_blob: new Uint8Array(0),
    output_blob: new Uint8Array(0),
    cron_id: new Uint8Array(0),
    pool_account: "",
    next_commission_time: "0",
    solver_id: new Uint8Array(0),
  };
}

export const InterPool = {
  $type: "flux.interpool.v1beta1.InterPool" as const,

  encode(message: InterPool, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pool_id.length !== 0) {
      writer.uint32(10).bytes(message.pool_id);
    }
    if (message.operator_addr !== "") {
      writer.uint32(18).string(message.operator_addr);
    }
    for (const v of message.inventory_snapshot) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.base_capital) {
      writer.uint32(34).string(v!);
    }
    if (message.operator_commission_config !== undefined) {
      CommissionConfig.encode(message.operator_commission_config, writer.uint32(42).fork()).ldelim();
    }
    if (message.operator_commission_fees !== undefined) {
      CommissionFees.encode(message.operator_commission_fees, writer.uint32(50).fork()).ldelim();
    }
    if (message.input_blob.length !== 0) {
      writer.uint32(58).bytes(message.input_blob);
    }
    if (message.output_blob.length !== 0) {
      writer.uint32(66).bytes(message.output_blob);
    }
    if (message.cron_id.length !== 0) {
      writer.uint32(74).bytes(message.cron_id);
    }
    if (message.pool_account !== "") {
      writer.uint32(82).string(message.pool_account);
    }
    if (message.next_commission_time !== "0") {
      writer.uint32(88).int64(message.next_commission_time);
    }
    if (message.solver_id.length !== 0) {
      writer.uint32(98).bytes(message.solver_id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InterPool {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInterPool();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pool_id = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operator_addr = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.inventory_snapshot.push(reader.string());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.base_capital.push(reader.string());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.operator_commission_config = CommissionConfig.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.operator_commission_fees = CommissionFees.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.input_blob = reader.bytes();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.output_blob = reader.bytes();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.cron_id = reader.bytes();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.pool_account = reader.string();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.next_commission_time = longToString(reader.int64() as Long);
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.solver_id = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InterPool {
    return {
      pool_id: isSet(object.pool_id) ? bytesFromBase64(object.pool_id) : new Uint8Array(0),
      operator_addr: isSet(object.operator_addr) ? globalThis.String(object.operator_addr) : "",
      inventory_snapshot: globalThis.Array.isArray(object?.inventory_snapshot)
        ? object.inventory_snapshot.map((e: any) => globalThis.String(e))
        : [],
      base_capital: globalThis.Array.isArray(object?.base_capital)
        ? object.base_capital.map((e: any) => globalThis.String(e))
        : [],
      operator_commission_config: isSet(object.operator_commission_config)
        ? CommissionConfig.fromJSON(object.operator_commission_config)
        : undefined,
      operator_commission_fees: isSet(object.operator_commission_fees)
        ? CommissionFees.fromJSON(object.operator_commission_fees)
        : undefined,
      input_blob: isSet(object.input_blob) ? bytesFromBase64(object.input_blob) : new Uint8Array(0),
      output_blob: isSet(object.output_blob) ? bytesFromBase64(object.output_blob) : new Uint8Array(0),
      cron_id: isSet(object.cron_id) ? bytesFromBase64(object.cron_id) : new Uint8Array(0),
      pool_account: isSet(object.pool_account) ? globalThis.String(object.pool_account) : "",
      next_commission_time: isSet(object.next_commission_time) ? globalThis.String(object.next_commission_time) : "0",
      solver_id: isSet(object.solver_id) ? bytesFromBase64(object.solver_id) : new Uint8Array(0),
    };
  },

  toJSON(message: InterPool): unknown {
    const obj: any = {};
    if (message.pool_id !== undefined) {
      obj.pool_id = base64FromBytes(message.pool_id);
    }
    if (message.operator_addr !== undefined) {
      obj.operator_addr = message.operator_addr;
    }
    if (message.inventory_snapshot?.length) {
      obj.inventory_snapshot = message.inventory_snapshot;
    }
    if (message.base_capital?.length) {
      obj.base_capital = message.base_capital;
    }
    if (message.operator_commission_config !== undefined) {
      obj.operator_commission_config = CommissionConfig.toJSON(message.operator_commission_config);
    }
    if (message.operator_commission_fees !== undefined) {
      obj.operator_commission_fees = CommissionFees.toJSON(message.operator_commission_fees);
    }
    if (message.input_blob !== undefined) {
      obj.input_blob = base64FromBytes(message.input_blob);
    }
    if (message.output_blob !== undefined) {
      obj.output_blob = base64FromBytes(message.output_blob);
    }
    if (message.cron_id !== undefined) {
      obj.cron_id = base64FromBytes(message.cron_id);
    }
    if (message.pool_account !== undefined) {
      obj.pool_account = message.pool_account;
    }
    if (message.next_commission_time !== undefined) {
      obj.next_commission_time = message.next_commission_time;
    }
    if (message.solver_id !== undefined) {
      obj.solver_id = base64FromBytes(message.solver_id);
    }
    return obj;
  },

  create(base?: DeepPartial<InterPool>): InterPool {
    return InterPool.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InterPool>): InterPool {
    const message = createBaseInterPool();
    message.pool_id = object.pool_id ?? new Uint8Array(0);
    message.operator_addr = object.operator_addr ?? "";
    message.inventory_snapshot = object.inventory_snapshot?.map((e) => e) || [];
    message.base_capital = object.base_capital?.map((e) => e) || [];
    message.operator_commission_config =
      (object.operator_commission_config !== undefined && object.operator_commission_config !== null)
        ? CommissionConfig.fromPartial(object.operator_commission_config)
        : undefined;
    message.operator_commission_fees =
      (object.operator_commission_fees !== undefined && object.operator_commission_fees !== null)
        ? CommissionFees.fromPartial(object.operator_commission_fees)
        : undefined;
    message.input_blob = object.input_blob ?? new Uint8Array(0);
    message.output_blob = object.output_blob ?? new Uint8Array(0);
    message.cron_id = object.cron_id ?? new Uint8Array(0);
    message.pool_account = object.pool_account ?? "";
    message.next_commission_time = object.next_commission_time ?? "0";
    message.solver_id = object.solver_id ?? new Uint8Array(0);
    return message;
  },
};

function createBasePoolShare(): PoolShare {
  return { pool_id: new Uint8Array(0), liquidity_provider_addr: "", asset_snapshot: [], base_capital: [], shares: "" };
}

export const PoolShare = {
  $type: "flux.interpool.v1beta1.PoolShare" as const,

  encode(message: PoolShare, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pool_id.length !== 0) {
      writer.uint32(10).bytes(message.pool_id);
    }
    if (message.liquidity_provider_addr !== "") {
      writer.uint32(18).string(message.liquidity_provider_addr);
    }
    for (const v of message.asset_snapshot) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.base_capital) {
      writer.uint32(34).string(v!);
    }
    if (message.shares !== "") {
      writer.uint32(42).string(message.shares);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PoolShare {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePoolShare();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pool_id = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.liquidity_provider_addr = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.asset_snapshot.push(reader.string());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.base_capital.push(reader.string());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.shares = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PoolShare {
    return {
      pool_id: isSet(object.pool_id) ? bytesFromBase64(object.pool_id) : new Uint8Array(0),
      liquidity_provider_addr: isSet(object.liquidity_provider_addr)
        ? globalThis.String(object.liquidity_provider_addr)
        : "",
      asset_snapshot: globalThis.Array.isArray(object?.asset_snapshot)
        ? object.asset_snapshot.map((e: any) => globalThis.String(e))
        : [],
      base_capital: globalThis.Array.isArray(object?.base_capital)
        ? object.base_capital.map((e: any) => globalThis.String(e))
        : [],
      shares: isSet(object.shares) ? globalThis.String(object.shares) : "",
    };
  },

  toJSON(message: PoolShare): unknown {
    const obj: any = {};
    if (message.pool_id !== undefined) {
      obj.pool_id = base64FromBytes(message.pool_id);
    }
    if (message.liquidity_provider_addr !== undefined) {
      obj.liquidity_provider_addr = message.liquidity_provider_addr;
    }
    if (message.asset_snapshot?.length) {
      obj.asset_snapshot = message.asset_snapshot;
    }
    if (message.base_capital?.length) {
      obj.base_capital = message.base_capital;
    }
    if (message.shares !== undefined) {
      obj.shares = message.shares;
    }
    return obj;
  },

  create(base?: DeepPartial<PoolShare>): PoolShare {
    return PoolShare.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PoolShare>): PoolShare {
    const message = createBasePoolShare();
    message.pool_id = object.pool_id ?? new Uint8Array(0);
    message.liquidity_provider_addr = object.liquidity_provider_addr ?? "";
    message.asset_snapshot = object.asset_snapshot?.map((e) => e) || [];
    message.base_capital = object.base_capital?.map((e) => e) || [];
    message.shares = object.shares ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
